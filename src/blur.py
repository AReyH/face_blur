def blur(sub_matrix,method='gaussian'):
    """
    This function receives the sub_array from find_face
    and given a blur method, it applies that blur to that
    sub_array, and returns the blurred sub_array.
    """
    pass
