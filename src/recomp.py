def recomp(matrix,sub_matrix):
    """
    This function receives the original image in an array
    form (matrix), and the sub_array (sub_matrix), and replaces
    the old array by adding the new sub_array on top of the old array.
    """
    pass
